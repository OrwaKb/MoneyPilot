[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # benign + test-only: starlette 1.3 prefers httpx2 for its TestClient; we
    # use httpx, which still works. Silenced so the suite stays clean.
    ignore:Using `httpx` with `starlette.testclient` is deprecated:Warning
