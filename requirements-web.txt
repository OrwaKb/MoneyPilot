# Web build dependencies (kept separate so the desktop install stays lean).
#
# IMPORTANT — starlette constraint: claude-agent-sdk pulls in sse-starlette,
# which requires a modern starlette (>=0.49). An old FastAPI (e.g. 0.115) pins
# starlette down to 0.41.x and BREAKS the AI integration. Keep FastAPI at a
# version whose starlette requirement is compatible, and never let starlette be
# downgraded below what the SDK needs.
fastapi==0.136.3
starlette>=1.3.1
uvicorn==0.34.0
httpx==0.28.1
python-multipart==0.0.20
itsdangerous==2.2.0
